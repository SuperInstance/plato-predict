[package]
name = "plato-predict"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Time series prediction primitives for PLATO tile streams"
repository = "https://github.com/SuperInstance/plato-predict"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }
